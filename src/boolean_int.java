public class boolean_int {
	Boolean bool_tag;//if there is a matching, bool_tag = true. Otherwise bool_tag = false;
	int count;//record the total number of comparisons after the algorithm ends;
	boolean_int()
	{
		bool_tag = false;
		count = 0;
	}
}